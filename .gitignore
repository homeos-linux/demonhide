target
# Tito build artifacts
/tmp/tito/
*.rpm
*.src.rpm
build/
# RPM development
rpmbuild/

# Note: vendor/ and .cargo/ are intentionally tracked for COPR builds
# They contain vendored Rust dependencies needed for offline builds